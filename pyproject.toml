[project]
name = "realtime"
version = "0.1.0"
description = "Workshop: Polling, Webhooks, SSE, and WebSockets - hands-on with two FastAPI projects and a Jupyter walkthrough."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "httpx>=0.27",
    "websockets>=12",
    "python-dotenv>=1.0",
    "openai>=1.30",
    "jupyter>=1.0",
]
